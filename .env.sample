# Credentials for the Postgres container in docker-compose.dev.yml.
# Copy to `.env`. These must match DB_USERNAME / DB_PASSWORD / DB_NAME
# in apps/backend/.env, which is how the backend connects to it.
POSTGRES_DB=worktrack
POSTGRES_USER=worktrack
POSTGRES_PASSWORD=worktrack
