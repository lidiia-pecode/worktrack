// `Input` gives its label no gap; this matches the app's other form labels.
export const settingsLabelClassName = "mb-1.5";

// Room for NumberInputControls, which replace the browser's own spin buttons.
export const settingsNumberInputClassName =
  "pr-12 [&::-webkit-inner-spin-button]:appearance-none " +
  "[&::-webkit-outer-spin-button]:appearance-none";
